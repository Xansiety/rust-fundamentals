fn main() {
    // Arrays
    // Arrays are fixed size and can't grow or shrink in size
    let items: [i32; 5] = [1, 2, 3, 4, 5];
    println!("The array is: {:?}", items);
    println!("The first item in the array is: {:?}", items[0]);
}
